# Python
__pycache__/
*.pyc

# PyInstaller build output (regenerated by CI / local builds)
build/
dist/
assets/icon.icns
assets/icon_master.png

# Runtime data (created on first run, not part of the source)
events.json
guardian_alerts.log
guardian_config.json

# macOS
.DS_Store
